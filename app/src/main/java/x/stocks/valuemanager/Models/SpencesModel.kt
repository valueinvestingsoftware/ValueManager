package x.stocks.valuemanager.Models

class SpencesModel(id: Int, spenceId: Int, spenceName: String, spence: Double, dSpences: String, dia: String, diaNumber: Int, diaSpences: Int, mSpences: String, monthNumber: Int, ySpences: Int, createdInApp: Boolean, bankDebit: Boolean, debitUniqueIdentifier: String) {
    var id = id

    var SpenceId = spenceId

    var SpenceName = spenceName

    var Spence = spence

    var DSpences = dSpences

    var dia = dia

    var diaNumber = diaNumber

    var diaSpences = diaSpences

    var MSpences = mSpences

    var monthNumber = monthNumber

    var YSpences = ySpences

    var createdInApp = createdInApp

    var BankDebit = bankDebit

    var DebitUniqueIdentifier = debitUniqueIdentifier

}