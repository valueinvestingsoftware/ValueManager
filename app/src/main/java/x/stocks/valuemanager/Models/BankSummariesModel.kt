package x.stocks.valuemanager.Models

class BankSummariesModel(id: Int, bankId: Int, ySpences: Int, balance: Double, monthname: String, monthNumber: Int) {

    var Id = id

    var BankId = bankId

    var YSpences = ySpences

    var Balance = balance

    var MonthName = monthname

    var MonthNumber = monthNumber

}