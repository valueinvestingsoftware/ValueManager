package x.stocks.valuemanager.Models

class SpenceCategoryModel(spenceId: Int, spenceDescription: String, createdInApp: Boolean) {
    var SpenceId = spenceId

    var SpenceDescription = spenceDescription

    var CreatedInApp = createdInApp
}